KeepRead

dépendances : 
 - python
 - dmenu (avec le patch password)
 - xclip
 - pykeepass (installer avec pip)
 - notify-send

Executez le script config.sh (./config.sh) afin d'éditer le chemin vers votre base de données.
